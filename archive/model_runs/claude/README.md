# Claude Run Overview

This archived model run uses the following native structure:

- `scripts/` - executable task scripts
- `reports/` - report documents and summary files
- `plots/` - saved EDA figures
- `metrics/` - evaluation outputs and model metadata
- `models/` - serialized trained models

For comparison against other model runs, interpret this folder using the common categories below:

- `code` -> `scripts/`
- `reports` -> `reports/`, `BENCHMARK_REPORT.md.docx`, `Read Me (Claude Code version).md`
- `plots` -> `plots/`
- `metrics` -> `metrics/`
- `logs` -> evidence is more report-centric than log-centric in this run
- `data` -> no dedicated archived data folder is present in this snapshot

This run is well evidenced, but it uses more `.docx` and `.xlsx` artifacts than the other archived runs.
